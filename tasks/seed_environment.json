{
  "description": "Seed a minimal resolved environment into the basedir directory for codavox to serve",
  "input_method": "environment",
  "parameters": {
    "basedir": {
      "description": "Basedir directory (r10k's basedir); defaults to /etc/puppetlabs/code/environments",
      "type": "Optional[String[1]]"
    },
    "environment": {
      "description": "Environment name to seed; defaults to production",
      "type": "Optional[String[1]]"
    }
  },
  "supports_noop": false
}
